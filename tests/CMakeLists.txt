function(add_test target)
    add_executable(${target} ${target}.cc)
    target_link_libraries(${target} gtest gtest_main udnn)
    gtest_discover_tests(${target})
endfunction()

add_test(test_layer)
add_test(test_simd)
add_test(test_tensor)
add_test(test_model)
add_test(test_quantize)
